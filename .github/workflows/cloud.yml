name: API Authentication Workflow

on:
  push:
    branches:
      - main

jobs:
  api:
    runs-on: ubuntu-latest

    steps:
      - name: Authenticate API
        run: |
          curl -H "Authorization: Bearer ${{ secrets.API_KEY }}" https://api.example.com
